# Search App

High-performance JavaScript search engine leveraging a Trie (prefix tree) data structure for fast, memory-efficient text lookup and autocomplete functionality. The system preprocesses dataset fields into a trie to support instant prefix-based searching, smart suggestions, and relevance-ranked results.

To launch the app on your local machine you should run `npm install && npm run dev`
